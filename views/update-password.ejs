<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update Password</title>

    <style>
          * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Roboto", sans-serif; /* Change to a modern font like Roboto */
        background: linear-gradient(to right, #3498db, #6f7bcc);
        margin: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }

      .container {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        gap: 20px;
        width: 100%;
      }

      .register {
        background: #ffffff;
        border: 1px solid #ddd;
        border-radius: 8px;
        padding: 20px;
        width: 300px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        animation: fadeIn 0.8s ease-in-out; /* Fade in animation */
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }

      h1 {
        font-size: 28px;
        color: #333;
        margin-bottom: 10px;
      }

      label {
        font-size: 14px;
        color: #333;
        margin-bottom: 6px;
      }

      input {
        padding: 10px;
        width: 100%;
        border: 1px solid #ddd;
        border-radius: 4px;
        margin-bottom: 10px;
        transition: border-color 0.3s ease-in-out;
      }

      input:focus {
        border-color: #3498db; /* Highlight border on focus */
      }

      .button-register {
        padding: 12px;
        background-color: #3498db;
        color: #fff;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: background-color 0.3s ease-in-out;
      }

      .button-register:hover {
        background-color: #2980b9;
      }

      a {
        color: #fff;
        text-decoration: none;
        font-size: 14px;
        transition: color 0.3s ease-in-out;
      }

      a:hover {
        color: #ecf0f1;
      }

      .register {
        display: flex;
        flex-direction: column;
        justify-content: center;
        gap: 8px;
      }

      .button-register {
        margin-top: 10px;
        padding: 3px;
      }

      input {
        padding: 4px;
      }

      .warn {
        color: red;
        font-size: 15px;
      }
    </style>
  </head>
  <body>
    <section class="container">
      <h1>Reset Password</h1>
      <form class="register" action="/api/auth/forgot-password" method="POST">
        <label for="email">Email</label>
        <input type="email" name="email" id="email" />

        <button class="button-register">Send Email Confirmation</button>

        <span class="warn">*Please check your email after send email confirmation</span>
      </form>
    </section>
  </body>

  <script></script>
</html>
